/*Add two numbers and display the result*/
#include <stdio.h>

int main() {
	int a, b;
	printf( "Enter two to be added: " );
	scanf( "%d %d", &a, &b );
	printf( "%d + %d = %d", a, b, a + b ); 
	 
	return 0;
}

